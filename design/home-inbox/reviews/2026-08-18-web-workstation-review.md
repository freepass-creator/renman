# 외부 AI 시안 검토 — 「렌터카 ERP 엔터프라이즈 웹 워크스테이션」 (2026-08-18, 웹 버전 · 참고)

- 대상: 사장님이 붙여넣은 웹 버전 HTML(Tailwind CDN + Lucide). 5만 자에서 잘려 도착 → 구조 기준 검토. 원본 파일은 저장 안 됨(붙여넣기 대신 파일로 받을 것)
- 기준: `SPEC.md` v3.1 · 홈 구현 1차(app/page.tsx, 2026-08-18)
- 모바일 버전 검토(같은 날)와 같은 계열의 시안 — 여기서는 웹 고유 항목만

## 1. 한 줄 판정

**원장(데이터센터) 쪽 아이디어는 쓸 만하고, 홈 쪽은 확정 방향(목록 + 수행창)과 다른 «대시보드 + 드로어» 형식이라 반려.**

## 2. 흡수 · 참고

| # | 시안의 요소 | 판단 | 위치 |
|---|---|---|---|
| A | 원장 조회 = 3대 원장(자산·계약·재무) → 세부 원장 탭 → **조밀한 표** + 검색 + 엑셀 내보내기 | 데이터센터 IA 참고로 좋음(원장은 표라는 확정 규칙과 일치). 홈 범위 밖 | 데이터센터 기획 시 |
| B | 업무 표에 「파싱 출처」 열 | v3.1 「근거 파일」로 이미 흡수 | SPEC §4-8 |
| C | 상단바 KPI 텔레메트리(가동률·수납률·할일) | 홈 목록 헤더의 지표 한 줄(보유·운행·휴차·가동률·미수)로 이미 있음. 상단바로 올리는 건 화면마다 반복이라 안 함 | — |
| D | 주간 스케줄 스트립(월~일 · 배차/수금/반납) | 시간축 섹션(오늘·이번 주)이 같은 질문에 답함. 별도 달력은 /desk(일정)이 담당. 보류 | 열린 질문 후보 아님 |

## 3. 반려 (확정 방향 위반)

| 항목 | 시안 | 확정 |
|---|---|---|
| 홈 | 「통합 관제」 KPI 카드 4장 + 긴급 업무 표 + 주간 달력 + 동기화 로그 대시보드 | 홈 = 업무(목록 + 수행창). 첫 화면을 지표 대시보드로 만들지 않는다(PRODUCT_DIRECTION §7·SPEC §1) |
| 상세 | 우측 슬라이드 **드로어**(max-w-lg) + 「원장 데이터 확정」 푸터 | 고정 **2-pane 수행창**(수행 | 차량360). 드로어는 목록과 수행창을 번갈아 가리는 구조라 처리 흐름이 끊김 |
| 업무 행 | ID/상태 배지 · 구분 · 문장형 과제명 · 계약번호 · 「어제 18:00 지연」 | 조치 앞머리 · 배지 없음 · 한 칸 한 원자(D+n) |
| 내비 | 4탭(통합 관제·업무 할일·원장 조회▾·데이터 올리기) + LIVE/카운트 배지 | 3앱(업무·데이터센터·자료올리기), 배지·구호 없음 |
| 시각 | 인디고 액센트 · 모노스페이스 대문자 헤더 · LIVE 펄스 · 카드 그리드 | 흰 면+헤어라인 · 색은 신호에만 · 카드 없음(SPEC §8) |
| 용어 | 할일·과제·관제·CORE SYSTEM·RENT-A-CAR ERP | 업무·조치·수행 · 영문 라벨 금지(§9) |

## 4. 다음 AI에게 다시 줄 때
- 홈은 «대시보드 아님, 목록 + 수행창» · 상세는 «드로어 아님, 고정 2-pane» · 조치 앞머리 · 3앱 · 용어 §9 를 제약에 명시
- 원장(데이터센터) 시안은 이 방향(3대 원장 → 세부 → 표)으로 계속 발전시켜도 됨 — 단 카드·배지 없이
- 파일로 저장해 `design/home-inbox/reviews/<ai>-<날짜>.html` 로 넣어 달라고 요청
