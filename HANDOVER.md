# 렌터카매니저 — 코덱스 인계 (2026-08-21)

대표: 「이거 개발은 천천히 오늘 밤에 하자 / 하던 거 이어서 **개발은 코덱스한테 맡길게**」

브랜치 **`reborn/2026-08-21`** · 지우기 전 상태는 태그 `archive/before-reset-2026-08-21`

---

## 1. 무엇을 만들었나

**뒤는 시트(정본) · 앞은 이 화면.** 시트를 버리지 않는다 —
화면이 죽어도 일이 돌아가야 하고, 대표가 직접 열어 볼 수 있어야 한다. 직원은 시트를 안 본다.

대표가 정한 동선 (그대로 지킬 것):

```
① 할 일 목록      「스위치플랜 미납고객 독촉 65건」
② 대상 한 줄씩    그 65명이 차량번호·이름·금액으로
③ 한 건 처리      무엇을 했는지 고르고 남긴다 → 운영 원장에 한 줄
④ 차량 원장       차량번호를 누르면 그 차의 운영·자금이 뜬다
```

> 「독촉 5건이면 그걸 누르면 독촉 5건에 대한 업무가 나와서 각각 처리하고 후속 뭘 했다고 남겨야지」
> 「차량 데이터 확인하려고 하면 그거 원장 확인하게 해주기 — 이거 erp 네 뭐....」

## 2. 파일

| 어디 | 무엇 |
|---|---|
| `app/page.tsx` | 할 일 목록 · 지표 · 검색 · 사람별 탭 |
| `components/OrderDetail.tsx` | ②③④ 세 걸음이 여기 다 있다 |
| `components/Login.tsx` | 회사 계정(파이어베이스 이메일·비번) |
| `lib/work/sheet.ts` | 업무내비 읽기·쓰기 · 행위 남기기 · 차량 원장 |
| `lib/work/targets.ts` | **오더 → 개별 대상** (수납 탭에서 뽑는다) |
| `lib/work/actions.ts` | 화면이 부르는 서버 함수 |
| `lib/work/people.ts` | 메일 ↔ 시트의 「담당」 이름 · 대표 판정 |
| `lib/work/types.ts` | 화면·서버 공용 타입 (server-only 를 클라이언트가 물면 번들이 막힌다) |
| `app/upload/` | 올리기 — 회사만 고르면 데이터센터 미분류로 |

## 3. 지금 실제로 도는 것

- 업무내비게이션 40건을 읽어 담당자별로 보여준다 (박태윤 18 · 권성민 19 · 강지수 2)
- **대상 뽑기가 실제로 된다** — 스위치 미납 59건 4,715만원 · 프라임 34건 2,021만원
- 완료·의견을 시트에 쓰고 운영 원장에도 남긴다
- 올리기는 데이터센터 폴더를 찾아 넣는다 (덮어쓰지 않고 (2) 를 붙인다)

## 4. 남은 일

| | 무엇 | 왜 |
|---|---|---|
| ① | **Vercel 배포** | 17번 시도 전부 「Deploying outputs」에서 실패. 빌드는 매번 성공하고 jpkerp5 도 같은 증상 → 계정 쪽 한도로 보인다. 풀리면 그대로 올라간다 |
| ② | `jpkerp.com` 도메인을 renman 으로 | 배포가 뜬 뒤에. 지금 옮기면 빈 화면이 뜬다 |
| ③ | **대상 못 뽑는 분류** | 지금은 미납·독촉·회수·과납·청구만. 보험증권·상환스케줄·계약서 받기는 시트로 넘긴다. `lib/work/targets.ts` 에 한 갈래씩 더한다 |
| ④ | 처리한 건을 목록에서 **되돌리기** | 잘못 눌렀을 때 취소가 없다 |
| ⑤ | 완료 두 단계 | 지금은 직원 체크만. 증빙 확인이 아직 화면에 없다 |

## 5. 건드리면 안 되는 것

- **직원이 쓰는 칸은 「완료」와 「담당자 의견」 둘뿐** — 다른 칸을 직원에게 열지 않는다
- **한 건 처리하면 반드시 운영 원장에 한 줄** — 그래야 나중에 입금·회수와 짝이 맞는다
- **조어를 쓰지 않는다** — 지연·완료·접수·연기·수행·원장·미수가 표준어다.
  「걸린 돈」 같은 말을 만들지 않는다 (대표 지적)
- **큰 글씨로 강조하지 않는다** — B2B 는 조밀하게. 위계는 굵기와 대비로
- 서비스계정 키(`renman-dd0a2-*.json`)를 커밋하지 않는다

## 6. 돌리는 법

```bash
npm run dev          # 6006. --turbo 는 쓰지 않는다(빌드 산출물과 엉켜 500)
npx tsc --noEmit
npm run build
```

`.env.local` 에 파이어베이스 값과 `GOOGLE_APPLICATION_CREDENTIALS` 가 있어야 시트를 읽는다.
Vercel 에는 `GOOGLE_SERVICE_ACCOUNT_KEY`(base64)·`GOOGLE_IMPERSONATE_USER` 로 들어가 있다.

## 7. 설계 정본

- 일의 생애 · 오더 구조 — `aiops/docs/WORK_LIFECYCLE.md`
- 짝(열린 일이 곧 오더) — `aiops/docs/OPEN_CLOSE.md` · 규칙표는 `aiops/lib/pairs.mjs`
- 약정·예정·사실 3층 — `aiops/docs/SCHEDULE_MODEL.md`
- 자금·운영 원장 — `aiops/docs/EVENT_LEDGER.md`
- 시트 ↔ 파이어베이스 정본 경계 — `aiops/docs/FIREBASE_BRIDGE.md`
- 막히면 `aiops/docs/INBOX.md` 에 한 줄
